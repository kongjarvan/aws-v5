server:
  port: 8081
  servlet:
    encoding:
      charset: utf-8

spring:
  datasource:
    url: jdbc:mariadb://database-1.c7etnij6qq1x.ap-northeast-2.rds.amazonaws.com:$3306/$metadb 
    driver-class-name: org.mariadb.jdbc.Driver
    username: ssar
    password: 12345678!

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  output:
    ansi:
      enabled: always

logging:
  level:
    '[org.springframework.boot.web]': DEBUG

# debug info warn error  